// The public API (createInteractionObserver and its types) lands in the next
// milestone. This entry point exists so the build, typecheck and test pipeline
// can be verified end to end first.
export {};
